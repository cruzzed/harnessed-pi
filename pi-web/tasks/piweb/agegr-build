#!/usr/bin/env bash
#MISE description="Build the pi-less-yolo-web2 image (agegr pi-web layer)"
set -euo pipefail

docker build -f "${MISE_TASK_DIR}/../../Dockerfile.agegr" -t pi-less-yolo-web2:latest "${MISE_TASK_DIR}/../.."
